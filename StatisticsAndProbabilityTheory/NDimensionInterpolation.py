"""
本代码完成一维、二维、三维下的线性插值。
【INTRODUCTION】对于一维线性插值，使用简单的截距式即可完成。而对于二维、三维线性插值，需要进行多步扩展。二维线性插值在数字图像图像处理中有很广泛的应用，
三维线性插值在物理化学领域也有着广泛的应用。
"""


def Interpolation_2D([x1,y1,z1],[x2,y2,z2],[x1,y2,z3],[x2,y1,z4],[x,y]):
    R1 = (x2 - x)/(x2 - x1)*z1 + (x - x1)/(x2 - x1)*z4
    R2 = (x2 - x)/(x2 - x1)*z3 + (x - x1)/(x2 - x1)*z2
    P = (y2 - y) / (y2 - y1)*R1 + (y - y1) / (y2 - y1)*R2
    return f(P)
